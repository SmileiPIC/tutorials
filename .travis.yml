
language: python

install:
 - git fetch -t
 - pip install Sphinx

script:
 - make html
 - touch _build/html/.nojekyll

deploy:
 - provider: pages
   skip-cleanup: true
   github-token: $GH_TOKEN
   keep-history: false
   local-dir: _build/html
